import { TopContainer } from "../components/containers";

const NotFoundPage = ()=> {
  return (
    <TopContainer className='flex text-4xl font-bold'>
      <h1 className='m-auto'>Page not found!</h1>
    </TopContainer>
  )
}

export default NotFoundPage;
